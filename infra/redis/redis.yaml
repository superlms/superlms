AWSTemplateFormatVersion: "2010-09-09"
Description: >-
  EdyOne LMS cache (Phase 7). ElastiCache Redis, cache.t4g.micro, single node,
  in the private subnets, reachable only from the ECS security group. Used for
  cache + sessions + queue (CACHE_STORE / SESSION_DRIVER / QUEUE_CONNECTION =
  redis). No auth token - it is private and SG-restricted.

Parameters:
  NodeType:
    Type: String
    Default: cache.t4g.micro

Resources:
  RedisSubnetGroup:
    Type: AWS::ElastiCache::SubnetGroup
    Properties:
      Description: EdyOne LMS private cache subnets
      SubnetIds: !Split [",", !ImportValue edyonelms-private-subnets]

  RedisCluster:
    Type: AWS::ElastiCache::CacheCluster
    Properties:
      ClusterName: edyonelms-redis
      Engine: redis
      EngineVersion: "7.1"
      CacheNodeType: !Ref NodeType
      NumCacheNodes: 1
      Port: 6379
      CacheSubnetGroupName: !Ref RedisSubnetGroup
      VpcSecurityGroupIds:
        - !ImportValue edyonelms-redis-sg

Outputs:
  RedisHost:
    Value: !GetAtt RedisCluster.RedisEndpoint.Address
    Export: { Name: edyonelms-redis-host }
  RedisPort:
    Value: !GetAtt RedisCluster.RedisEndpoint.Port
    Export: { Name: edyonelms-redis-port }
